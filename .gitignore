# Python
__pycache__/
*.pyc
.venv/
*.db

# Node / Next.js
node_modules/
.next/
frontend/.env.local

# Env
.env

# 대용량 아카이브 (GitHub 100MB 한도 초과 — 히스토리에 넣지 않는다)
test.zip
*.zip

# 생성·업로드 산출물 바이너리 (런타임 파일, 히스토리에 넣지 않는다)
# storage/model/registry.json 은 런타임에 필요하므로 계속 추적한다.
storage/results/
storage/uploads/
storage/model/*.jpg
storage/model/*.mp4
storage/model/*.png

# OS
.DS_Store
Thumbs.db
